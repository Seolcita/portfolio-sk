import { ReactElement } from 'react';

import * as S from './projects.styles';

export const Projects = (): ReactElement => {
  return (
    <S.Container>
      <h1>Projects</h1>
    </S.Container>
  );
};
